Place the exported TensorFlow Lite models here.

Required files:
- my_model/my_model.tflite
- my_model/labels.txt

Expected label format per line:
<armor-id>

Example:
141

Only these runtime files are needed by the APK.